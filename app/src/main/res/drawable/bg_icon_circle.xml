<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Circle fill -->
    <solid android:color="@color/maroon" />

    <!-- Slight thin border for definition -->
    <stroke
        android:width="1dp"
        android:color="@color/light_gray" />

    <!-- size will be defined by ImageView layout params; padding handled in ImageView -->
</shape>
